/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Removed experimental.appDir as it's not needed in Next.js 14
  // and causes a warning
}

module.exports = nextConfig
